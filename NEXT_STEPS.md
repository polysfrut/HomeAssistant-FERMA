# 📋 ЧТО НУЖНО СДЕЛАТЬ ДАЛЬШЕ

## ✅ Шаг 1: Синхронизация изменений на сервере Home Assistant

**Подключитесь к серверу через SSH или используйте аддон "SSH & Web Terminal":**

```bash
cd /config
git pull origin main
```

## ✅ Шаг 2: Проверка конфигурации

**Проверьте, что конфигурация корректна:**

```bash
ha core check
```

Если есть ошибки - исправьте их перед перезагрузкой.

## ✅ Шаг 3: Перезагрузка Home Assistant

**Перезагрузите Home Assistant для применения изменений:**

```bash
ha core restart
```

Или через веб-интерфейс:
- Настройки → Система → Перезагрузка

## ✅ Шаг 4: Проверка работы

**После перезагрузки проверьте:**

1. **Откройте Dashboard:**
   - Перейдите в Dashboard → Брудер 2
   - Проверьте, что все элементы отображаются

2. **Проверьте режимы:**
   - Включите "Ручной режим" → должна появиться панель "Управление реле"
   - Включите "Автоматический режим" → "Ручной режим" должен выключиться

3. **Проверьте кнопку "Прогрев":**
   - Включите "Ручной режим"
   - Нажмите "Прогрев" → должны включиться все ИК лампы и ТЭНы
   - Дождитесь достижения max_temp → ТЭНы должны отключиться автоматически

4. **Проверьте автоматизации:**
   - Включите "Автоматический режим"
   - При достижении min_temp должен включиться ТЭН1
   - При достижении max_hum должен включиться вентилятор

5. **Проверьте защиту реле:**
   - Выключите оба режима (ручной и автоматический)
   - Попробуйте переключить любое реле → должно заблокироваться с уведомлением

## ⚠️ ВАЖНО: Проверка датчиков

**Обнаружено несоответствие:**
- В автоматизациях используется: `sensor.waveshare001_rs485_temperature_10`
- В dashboard используется: `sensor.waveshare001_rs485_temperature_11`

**Проверьте, какой датчик правильный:**

```bash
ha core states list | grep "sensor.waveshare001_rs485_temperature"
ha core states list | grep "sensor.waveshare001_rs485_humidity"
```

Если правильный датчик `_11`, нужно обновить автоматизации.
Если правильный датчик `_10`, нужно обновить dashboard.

## 📝 Следующие шаги (опционально)

1. **Настройка уведомлений:**
   - Проверьте, что уведомления приходят в Telegram
   - Настройте chat_id в `packages/brooder.yaml` если нужно

2. **Тестирование всех сценариев:**
   - Протестируйте работу ТЭНов при разных температурах
   - Протестируйте работу вентилятора при разной влажности
   - Протестируйте защиту от переохлаждения

3. **Мониторинг:**
   - Следите за логами: Настройки → Система → Журналы
   - Проверяйте историю на графиках

## 🆘 Если что-то не работает

1. **Проверьте логи:**
   ```bash
   ha core check
   ```

2. **Проверьте состояние entities:**
   ```bash
   ha core states list | grep input_boolean
   ha core states list | grep input_number
   ```

3. **Проверьте автоматизации:**
   - Настройки → Автоматизации и сцены → Автоматизации
   - Убедитесь, что все автоматизации включены

4. **Очистите кэш Lovelace (если dashboard не обновляется):**
   ```bash
   rm -f .storage/lovelace*
   ```
   Затем обновите страницу в браузере (Ctrl+F5)

